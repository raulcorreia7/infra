HOST_NAME=athena
PUBLIC_FQDN=
TAILNET_DOMAIN=
HOME_DOMAIN=
HOME_DNS_RESOLVER=
TZ=Europe/Amsterdam
EDGE_NETWORK=edge

# Optional: keep this set if you want a stable Headplane cookie secret.
# HEADPLANE_COOKIE_SECRET=

# Optional: comma-separated Headscale users to create after startup.
# HEADSCALE_SEED_USERS=
